# 320-wealist-extended-k8s

## Git 설정 (필수)

**반드시 아래 설정을 준수할 것:**

```bash
git config --local user.name "212clab"
git config --local user.email "212clab@gmail.com"
```

- 커밋 author/committer는 반드시 `212clab`만으로 설정
- Claude나 다른 이름으로 커밋 금지
